name: Install Rust toolchain
description: Install the repository-pinned Rust toolchain for Harn source-install fallbacks.
runs:
  using: composite
  steps:
    - name: Install repository Rust toolchain
      shell: bash
      run: |
        set -euo pipefail
        rustup show
        rustc -Vv
        cargo -V
