Your previous answer did not match the required output format.

Return only the final audit bullets now. Every non-empty line must start with
"- ". Do not include analysis, headings, or explanations.

Bounded fleet context:
```json
{{ context_json }}
```
Bullets:
