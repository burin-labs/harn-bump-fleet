Release handoff failure context:
```json
{{ failure_context_json }}
```

Use any structured `live_log_excerpt.markdownlint` fields before broad reads.
For markdown/prose lint repairs, prefer the existing `release_replace_file`,
avoid whole-file rewrites, and verify with one bounded lint command afterward.
If additional reads are necessary, explain the specific missing fact.

Diagnose this hiccup from local evidence and recommend the next concrete action.
