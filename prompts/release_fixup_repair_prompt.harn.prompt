Write the final fixup audit from the deterministic facts below. This is
a fresh final-answer turn with no tools. Do not mention the validation
process or any earlier response.

Fix these machine-checked findings:
{{ validation_findings }}

Constraints (still in force from the original prompt):

- The release artifact for `{{ target_version }}` already shipped from
  tag `{{ fixup_tag }}`. Do not author release notes. Do not include
  `BEGIN_DRAFT_CHANGELOG` markers.
- Do not recommend retagging or re-running `publish-release.yml`.
- The harness preserved `## Unreleased` as-is — it represents the next
  release's content, not this one's. Do not recommend folding
  Unreleased into v{{ target_version }}.

Your first non-whitespace characters must be exactly `Audit:`. Use this
shape:

Audit:
- Shipped v{{ target_version }}: confirm the injected body in
  CHANGELOG.md matches what shipped on `{{ fixup_tag }}` (no edits
  crept in).
- Held in `## Unreleased`: 1-3 entries; confirm they describe work that
  landed AFTER the tag and belong in the next release.
- Tag/artifact: confirm the harness left the tag and published release
  alone.
- Misclassification flags: list `## Unreleased` entries that should
  actually be in v{{ target_version }}, or empty if none.

Stop after the last Audit bullet.

Release fact sheet:
{{ release_fact_sheet }}
