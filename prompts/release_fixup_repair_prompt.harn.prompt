Write the final fixup audit from the deterministic facts below. This is
a fresh final-answer turn with no tools. Do not mention the validation
process or any earlier response.

Fix these machine-checked findings:
{{ validation_findings }}

Constraints (still in force from the original prompt):

- The recovered release notes for `{{ target_version }}` are authoritative.
  Do not author release notes or include `BEGIN_DRAFT_CHANGELOG` markers.
- Do not recommend moving an existing tag or re-publishing an artifact.
- The harness preserved post-pin `## Unreleased` entries as the next release's
  content. Do not recommend folding them into v{{ target_version }}.

Your first non-whitespace characters must be exactly `Audit:`. Use this
shape:

Audit:
- Release v{{ target_version }}: confirm the injected body matches the recovered
  release content and, when present, tag `{{ fixup_tag }}`.
- Held in `## Unreleased`: 1-3 entries; confirm they describe post-pin work
  outside the recovered release and belong in the next release.
- Tag/artifact: confirm any existing tag and published release were left alone;
  otherwise state that deterministic publication remains pending.
- Misclassification flags: list `## Unreleased` entries that should
  actually be in v{{ target_version }}, or empty if none.

Stop after the last Audit bullet.

Release fact sheet:
{{ release_fact_sheet }}
