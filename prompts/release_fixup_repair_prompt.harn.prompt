Write the final fixup audit from the deterministic facts below. This is a
fresh final-answer turn with no tools. Do not mention the validation process.

Fix these machine-checked findings:
{{ validation_findings }}

The selected release-note source is authoritative. Do not author release
notes, include draft markers, move an existing tag, or recommend republishing
an immutable artifact.
{{ if candidate_tree_notes -}}
The fresh candidate's current `## Unreleased` and every fragment belong in
v{{ target_version }}; flag any residual note or missing source coverage.
{{ else -}}
The frozen artifact body belongs in v{{ target_version }}; later notes remain
in `## Unreleased` for the next release.
{{ end -}}

Your first non-whitespace characters must be exactly `Audit:`. Cover:

- Release v{{ target_version }}: selected-source fidelity and source coverage.
- Remaining notes: empty for a fresh candidate, or correctly deferred for a
  frozen artifact.
- Tag/artifact: immutable state preserved or deterministic publication pending.
- Misclassification flags: exact entries, or empty.

Stop after the last Audit bullet.

Release fact sheet:
{{ release_fact_sheet }}
