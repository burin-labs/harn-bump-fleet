# Harn release harness run {{ run_id }}

- **Started:**        {{ started_iso }}
- **Finished:**       {{ finished_iso }}
- **Operator:**       {{ operator }}@{{ host }}
- **Repo:**           {{ analysis.repo }}
- **Mode:**           {{ analysis.mode }}
- **Mock:**           {{ analysis.mock }}
- **Bump:**           {{ analysis.bump }}
- **Provider:**       {{ analysis.provider }}
- **Version:**        {{ analysis.current_version }} -> {{ analysis.next_version }}
- **Branch:**         {{ analysis.branch }} (base `{{ analysis.base_branch }}`, expected `{{ analysis.expected_branch }}`)
- **Latest tag:**     {{ analysis.latest_tag | default: "n/a" }}
- **Audit hash:**     `{{ audit_sha }}`

## Deterministic findings

{{ if analysis.deterministic_findings -}}
{{ for f in analysis.deterministic_findings -}}
- {{ f }}
{{ end -}}
{{ else -}}
- none
{{ end -}}

## Run/session view compatibility

- **Fixture check:** {{ if analysis.run_session_view.check.success }}passed{{ else }}failed{{ end }} (`{{ analysis.run_session_view.check.command | default: "make check-run-view-fixtures" }}`)
- **Tested schemas:** {{ analysis.run_session_view.summary_line | default: "unknown" }}
- **Release note:** {{ if analysis.run_session_view.release_note.required }}{{ if analysis.run_session_view.release_note.present }}required and present{{ else }}required but missing{{ end }}{{ else }}not required{{ end }}

## Changed files

```text
{{ analysis.status | default: "(clean)" }}
```

## Diff stat

```text
{{ analysis.diff_stat | default: "(no diff)" }}
```

## Diff names

```text
{{ analysis.diff_names | default: "(none)" }}
```

## Commits since latest tag

```text
{{ analysis.commits_since_latest_tag | default: "(none)" }}
```

## Commit file changes since latest tag

```text
{{ analysis.commit_file_changes_since_latest_tag | default: "(none)" }}
```

## Top changelog section

```text
{{ analysis.changelog_section | default: "(empty)" }}
```

## Unreleased changelog section

```text
{{ analysis.changelog_unreleased_section | default: "(empty)" }}
```

## Changelog gap candidates

```text
{{ analysis.changelog_gap_candidates | default: "(none)" }}
```

## Agent review

{{ agent_review }}

{{ if agent_review_detail.validation_findings -}}
## Agent review validation

{{ for f in agent_review_detail.validation_findings -}}
- {{ f }}
{{ end -}}
{{ end -}}

{{ if agent_review_detail.artifacts.raw_result_json -}}
## Agent artifacts

- Raw result: `{{ agent_review_detail.artifacts.raw_result_json }}`
- Trace: `{{ agent_review_detail.artifacts.trace_json }}`
- Transcript context: `{{ agent_review_detail.artifacts.transcript_context_json }}`
- LLM JSONL transcript: `{{ agent_review_detail.artifacts.llm_transcript_jsonl }}`
- Draft changelog: `{{ agent_review_detail.artifacts.draft_changelog_md }}`
{{ end -}}

## Execution

{{ if execution -}}
| Step | Status | Classification | Command |
|------|--------|----------------|---------|
{{ for s in execution -}}
| {{ s.name }} | {{ if s.success }}ok{{ else }}failed ({{ s.status }}){{ end }} | `{{ s.classification | default: "" }}` | `{{ s.command }}` |
{{ end -}}
{{ else -}}
No side-effecting release step was run.
{{ end -}}

{{ if execution -}}
{{ for s in execution -}}
{{ if s.remote_log_path -}}
## Remote audit offload

- **Host:** `{{ s.remote_host }}`
- **Remote dir:** `{{ s.remote_dir }}`
- **Remote log:** `{{ s.remote_log_path }}`
- **Setup status:** {{ s.setup_status | default: "n/a" }}
- **Audit status:** {{ s.audit_status | default: "n/a" }}

{{ if s.diagnostic_output -}}
```text
{{ s.diagnostic_output }}
```
{{ end -}}
{{ end -}}
{{ end -}}
{{ end -}}

{{ if execution -}}
{{ for s in execution -}}
{{ if s.platform_certification_receipt -}}
## Hosted platform certification

- **Source:** `{{ s.platform_certification_receipt.source.repo }}@{{ s.platform_certification_receipt.source.sha }}`
- **Branch freeze:** `{{ s.platform_certification_receipt.source.branch }}` remained `{{ s.platform_certification_receipt.source.remote_sha_after }}`
- **Critical path:** `{{ s.platform_certification_receipt.join.critical_path }}` ({{ s.platform_certification_receipt.join.duration_ms }} ms)
- **Warm binary:** `{{ s.platform_certification_receipt.warm.binary_sha256 }}` from
  `{{ s.platform_certification_receipt.warm.command }}`
- **Receipt:** `{{ s.platform_certification_receipt_path }}`

| Platform | Workflow | Run | Duration |
|----------|----------|-----|----------|
{{ for p in s.platform_certification_receipt.platforms -}}
| {{ p.platform }} | `{{ p.workflow_path }}` | [{{ p.run.id }}]({{ p.run.url }}) | {{ p.duration_ms }} ms |
{{ end -}}

{{ end -}}
{{ end -}}
{{ end -}}

## Workflow reminder

The intended Harn release flow is one human-authored PR titled
`Release vX.Y.Z`. After that PR lands through the merge queue, GitHub Actions
publish the release, push the tag, and build binaries/container artifacts.
