Write the final Harn release audit from the deterministic facts below. This is
a fresh final-answer turn. Do not mention these requirements, internal checks,
reviewer feedback, or that you are fixing an earlier response.

Hard requirements for the final answer:
{{ validation_findings }}
- If the first post-tag commit in the release fact sheet is `(none)`, state
  that there are no post-tag commits and do not invent release-note bullets from
  recent merged PRs. If it is not `(none)`, the final answer must contain that
  commit hash.
- Before writing the draft, privately decide include, consolidate, or omit for
  every post-tag commit in the release fact sheet. Do not print that matrix.
  The final draft should cover each user-visible, harness-writer-visible,
  install, package, publish, release, CLI, protocol, stdlib, or behavior change,
  either explicitly or by a clearly related consolidated bullet. Omit routine
  dependency bumps and low-level churn only when they have no release-note
  impact.

Do not call tools in this turn. There are no tools available. Use only the
compact deterministic release analysis below to write the final audit from
scratch. Ignore any prior answer shape; it is intentionally omitted.

Release fact sheet:
{{ release_fact_sheet }}

If the first post-tag commit has user-visible release or install impact,
include it in the draft. Do not drop it just because it is recent or
internal-looking.

The draft changelog block must begin exactly:

BEGIN_DRAFT_CHANGELOG
## {{ target_version }}

Never use `## Unreleased` inside the draft block. Preserve authored Unreleased
entries by promoting and consolidating their content under `## {{ target_version }}`.
When there are no post-tag commits and no authored Unreleased entries, the
draft block should contain only the target version heading and no invented
bullets.

Return the concise release audit now. Your first non-whitespace characters must
be exactly `Audit:`. Include the expected next version, CHANGELOG coverage
decisions, release quality risks, and the next concrete command. Do not answer
with a meta statement about preparing, compiling, checking, or writing the
audit. Also include a ready-to-paste markdown changelog section between
BEGIN_DRAFT_CHANGELOG and END_DRAFT_CHANGELOG markers.
Use the audit bullet shape: Version/readiness, CHANGELOG coverage, First
post-tag commit, Release risks, Next command. The First post-tag commit bullet
must contain `{{ first_post_tag_commit }}`.
Do not write headings or sentences named Validation, Requirements, Reviewer
feedback, or Deterministic facts in the final answer.
Do not include Markdown code fences anywhere in your answer. The draft markers
are plain marker lines, not code fences. If you mention the next command, write
it inline outside the changelog block.
After END_DRAFT_CHANGELOG, stop; do not add trailing commentary.

The Harn repo does not have `scripts/release_prepare.sh`; do not mention it.
Release preparation is driven by this harness and `./scripts/release_ship.sh
--prepare --bump <type>`. The next live command should be the harness
`ship-pr` mode, not manual branch/tag/publish steps.

If the prior answer recommended `git checkout`, `git commit`, `git push`,
opening a PR manually, merging manually, tagging manually, or publishing
manually, replace that section. The correct next command is `harn run
release_harn.harn -- --mode ship-pr --agent --yes-live-release`.

Keep the changelog draft conservative: do not merge distinct named lints or
grammar features, do not include unsupported implementation claims, and do not
write literal compact tool-protocol angle-bracket tags. Say "compact text-tool
aliases" if that change needs coverage.
Do not use angle-bracket placeholders in the draft changelog, even inside
inline code. Markdown lint treats placeholders like `harn serve mcp <script>`
as inline HTML; write `harn serve mcp SCRIPT` instead.
Respect repository markdown lint rules in the draft, including wrapping prose
lines below the configured line-length limit with valid markdown continuation
indentation.

Keep the corrected draft focused enough for a human release note: prefer 6-10
top-level bullets, use 12 as the normal hard cap, and never exceed 20. Merge
closely related agent-loop, workflow, transport, and persona items into
coherent bullets. Omit routine dependency bumps, CI churn, and deep internal
plumbing unless the change affects a harness writer or Harn user. Do not create
duplicate Added/Changed/Fixed sections for the same topic; one section is fine
when it is clearer.
Do not create vague catch-all bullets such as "Other additions" or
"Miscellaneous"; either name a concrete user-facing capability or omit the
lower-signal item.
Do not narrow the draft to only the first validation failure. Use the full
post-tag commit list and per-commit file evidence to compile broad release-note
coverage, while still omitting routine dependency bumps and low-level churn
without release-note impact.
Do not carry forward notes from an older release for continuity: if a note was
already released, omit it instead of saying it was already present.

If CHANGELOG.md already had a non-empty `## Unreleased` section in the release
context, preserve those entries in the draft while adding missing post-tag
coverage. Do not replace authored Unreleased notes with a narrower rewrite.

In ship-pr mode, do not claim the PR is merged, the tag exists, release
publishing has happened, or finalization should run unless that was explicit
evidence in the deterministic payload. Do not invent aggregate diff totals,
file counts, insertion/deletion counts, or CI results.

A commit titled `Release vX.Y.Z` means only that the release branch has a
release commit. It is not evidence that the PR merged, a tag was created, or
publishing ran.
