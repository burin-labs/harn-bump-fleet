Release v{{ next_version }}{{ fixup_note }}

This PR was prepared by `release_harn.harn` from a fresh release snapshot.
{{ preparation_note }}

## Post-prepare summary

- Version: `{{ current_version }}` -> `{{ next_version }}`
- Branch: `{{ branch }}` targeting `{{ base_branch }}` (parented at pin SHA below)
- Pinned base SHA: `{{ pin_sha }}` {{ pin_sha_note }}
- Tag pushed for publish: `v{{ next_version }}` — {{ tag_publish_note }}
- Latest tag used for release delta: `{{ latest_tag }}`
- Changed files: `{{ changed_files }}`
- Deterministic findings: {{ deterministic_findings }}

## Changelog section

```markdown
{{ changelog_section }}
```

## Commits covered

```text
{{ commits_since_latest_tag }}
```

## Execution transcript

{{ execution_summary }}

## Agent artifacts

- Audit JSONL transcript: `{{ audit_transcript }}`
- Draft changelog: `{{ draft_changelog }}`
- Agent validation findings: {{ validation_findings }}

Generated by `release_harn.harn`.
