The deterministic CHANGELOG fold for v{{ target_version }} produced a malformed
release section and needs a bounded repair.

You are given the authoritative release-note fragments and the current (invalid)
section. Reorganize the fragment bullets into one valid Keep-a-Changelog
`## v{{ target_version }}` section. Do not invent new bullets, do not drop any
fragment bullet, and do not add commentary — every bullet in your output must
come from the fragments below.

Authoritative fragment content:

{{ fragments_section }}

Current invalid section (for context only; do not copy its structure blindly):

{{ current_section }}

Output requirements:

- Emit only CHANGELOG markdown. Start with the heading `## v{{ target_version }}`.
- Group bullets under `### Added`, `### Changed`, `### Fixed`, etc., matching
  each fragment's category.
- One blank line after every `## ...` and `### ...` heading, one blank line
  between subsections.
- Keep every fragment bullet's meaning intact; you may only regroup and format.
- No code fences, no placeholders, no prose outside the section.
