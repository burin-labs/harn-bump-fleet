You used local tools successfully and have enough evidence for this release
audit. No tools are available on this final prose turn. Return only final
user-facing text now. Do not write planning notes, evidence analysis, "I need
to..." text, or a description of what you are about to do. Your first
non-whitespace characters must be exactly `Audit:`.

Hard requirements for the final answer:
- Target version: `{{ target_version }}`.
- Latest tag/base range: `{{ tag_range }}`.
- Mention the first post-tag commit exactly as evidence:
  `{{ first_post_tag_commit }}`.
- If the first post-tag commit is `(none)`, state that there are no post-tag
  commits and do not invent release-note bullets from recent merged PRs. If it
  is not `(none)`, the final answer must contain that commit hash.
- The normal next command is `harn run release_harn.harn -- --mode ship-pr --agent --yes-live-release`.
- Before writing the draft, make a private include/consolidate/omit decision for
  every post-tag commit in the release fact sheet. Do not print that matrix.
  The final draft should cover each user-visible, harness-writer-visible,
  install, package, publish, release, CLI, protocol, stdlib, or behavior change,
  either explicitly or by a clearly related consolidated bullet. Omit routine
  dependency bumps and low-level churn only when they have no release-note
  impact.

Use this compact shape:

Audit:
- Version/readiness: one bullet.
- CHANGELOG coverage: one bullet.
- First post-tag commit: one bullet containing `{{ first_post_tag_commit }}`.
- Release risks: one or two bullets.
- Next command: one bullet naming this harness in ship-pr mode.

Then include BEGIN_DRAFT_CHANGELOG / END_DRAFT_CHANGELOG. Do not include a
coverage matrix, commit-by-commit analysis, planning prose, or long risk essay.
Do not write headings or sentences named Validation, Requirements, Reviewer
feedback, or Deterministic facts in the final answer.
The draft heading must be `## {{ target_version }}`, not `## Unreleased`.
The draft should have 6-10 top-level bullets, use 12 as a normal hard cap, and
never exceed 20; preserve the factual content of authored Unreleased entries by
promoting and consolidating them under the version heading. Group missing
post-tag work into concise user-facing bullets and omit routine dependency
churn. The BEGIN_DRAFT_CHANGELOG block must contain only CHANGELOG markdown;
when there are no post-tag commits and no authored Unreleased content, it should
contain only the target version heading and no invented bullets.
respect the repository markdown lint rules, including wrapping prose lines below
the configured line-length limit with valid continuation indentation; put the
next command in the Audit bullets outside the block. After
END_DRAFT_CHANGELOG, stop; do not add trailing commentary.

Release fact sheet:
{{ release_fact_sheet }}
