You used local tools successfully and have enough evidence for this release
audit. No tools are available now. Return only final user-facing text. Your
first non-whitespace characters must be exactly `Audit:`.

Use this shape:

Audit:
- Version/readiness: target `{{ target_version }}` from `{{ tag_range }}`.
- CHANGELOG coverage: summarize whether the draft preserves and completes the
  release-note evidence, and ground it in concrete scope. This line MUST cite
  either a specific `#`-prefixed post-tag PR (e.g. `{{ sample_pr }}`) or the
  post-tag PR count (`{{ post_tag_pr_count }}` PRs) — a generic coverage line
  that names neither is automatically rejected.
- First post-tag commit: include `{{ first_post_tag_commit }}` exactly.
- Release risks: one or two concise bullets.
- Next command: name `harn run --no-sandbox release_harn.harn -- --mode ship-pr --agent --yes-live-release`.

Then include a ready-to-paste changelog section between
BEGIN_DRAFT_CHANGELOG and END_DRAFT_CHANGELOG. After END_DRAFT_CHANGELOG, stop.

Strict output rules (any violation auto-rejects the audit):
- First non-whitespace characters are exactly `Audit:`.
- NO Markdown code fences (```) anywhere in the response — write the `Next
  command` and any code inline in single backticks, and keep
  `BEGIN_DRAFT_CHANGELOG` / `END_DRAFT_CHANGELOG` as bare lines on their own.
- The draft between the markers starts with the heading `## {{ target_version }}`
  and contains no `## Unreleased` heading.
- The `CHANGELOG coverage` line cites a `#`-prefixed PR or the PR count (above).
- Include the exact first-post-tag commit hash `{{ first_post_tag_commit }}`.
- Do not mention `scripts/release_prepare.sh` or `--finalize`.
- Output nothing after `END_DRAFT_CHANGELOG`.

{{ changelog_draft_instructions }}

Release fact sheet:
{{ release_fact_sheet }}
