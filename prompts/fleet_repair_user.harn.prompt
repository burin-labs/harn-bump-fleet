Repair the failed Harn runtime bump for {{ repo }} at {{ target }}.

Pull request: #{{ pr_number }} ({{ pr_url }})
Leased head: {{ head_sha }}
Exact failing-check evidence:
{{ failing_checks_json }}

Prior deterministic validation findings, if this is a retry:
{{ prior_findings }}

First inspect the failed check logs and the exact bump diff, then make the smallest source repair that addresses the evidenced incompatibility. If the evidence names a relative source path, read that path before guessing a different location. Leave the checkout ready for the repository-owned deterministic validation command. Do not commit or publish.
