Compact release analysis JSON follows as raw JSON:
{{ analysis_json }}

Run a short evidence check that includes at least one release-delta command and
one content/PR/diff inspection, then wait for the finalization turn to write the
audit and changelog draft. The draft should synthesize concrete behavior from
the gathered evidence, not restate commit titles.
