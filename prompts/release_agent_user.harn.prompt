Compact release analysis JSON follows as raw JSON:
{{ analysis_json }}

Use the typed exact-range and concrete-content evidence in the analysis. Read
more only if a material fact is missing, then wait for the finalization turn to
write the audit and changelog draft. The draft should synthesize concrete
behavior from the evidence, not restate commit titles.
