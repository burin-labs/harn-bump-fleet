You used local tools successfully and have enough evidence for this release
recovery audit. No tools are available now. Return only final user-facing text
whose first non-whitespace characters are exactly `Audit:`.

Use this shape:

Audit:
- Release v{{ target_version }}: confirm the prepared body matches the selected
  release-note source and the source tree it describes.
{{ if candidate_tree_notes -}}
- Remaining notes: confirm `## Unreleased` and `changelog.d` are empty after the
  fresh candidate fold, or name the exact residual entry as a blocker.
{{ else -}}
- Remaining notes: name representative entries held for the next release and
  confirm they landed after the frozen artifact.
{{ end -}}
- Tag/artifact: confirm immutable state was left alone, or that deterministic
  publication remains pending.
- Misclassification flags: exact entries, or empty.

Do not include draft markers. Stop after the last Audit bullet.

Release fact sheet:
{{ release_fact_sheet }}
