You used local tools successfully and have enough evidence for this
post-publish fixup audit. No tools are available now. Return only final
user-facing text. Your first non-whitespace characters must be exactly
`Audit:`.

Use this shape:

Audit:
- Shipped v{{ target_version }}: confirm the injected body in
  CHANGELOG.md matches what actually shipped on tag `{{ fixup_tag }}`
  (no edits crept in; nothing extra was folded).
- Held in `## Unreleased`: name 1-3 entries currently in Unreleased
  and confirm they describe work that landed AFTER the tag (so they
  belong in the NEXT release, not this one).
- Tag/artifact: confirm the harness left the tag and the published
  release alone (no retag, no republish).
- Misclassification flags: list `## Unreleased` entries that should
  actually be in v{{ target_version }} (rare — would be a fix to
  shipped behavior), or empty if none. Operator must manually move
  any flagged entries.

Do not include `BEGIN_DRAFT_CHANGELOG` or any draft block. The shipped
release notes are authoritative; the harness's job here is to land the
PR, not to author. Stop after the last Audit bullet.

Release fact sheet:
{{ release_fact_sheet }}
