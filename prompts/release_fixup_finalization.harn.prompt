You used local tools successfully and have enough evidence for this
release-recovery audit. No tools are available now. Return only final
user-facing text. Your first non-whitespace characters must be exactly
`Audit:`.

Use this shape:

Audit:
- Release v{{ target_version }}: confirm the injected body is exactly the
  recovered release content; when tag `{{ fixup_tag }}` exists, confirm it
  matches what shipped.
- Held in `## Unreleased`: name 1-3 entries currently in Unreleased
  and confirm they describe work that landed AFTER the tag (so they
  belong in the NEXT release, not this one).
- Tag/artifact: confirm any existing tag and published release were left alone;
  otherwise state that deterministic publication remains pending.
- Misclassification flags: list `## Unreleased` entries that should
  actually be in v{{ target_version }} (rare — would correct the
  recovered release content), or empty if none. Operator must manually move
  any flagged entries.

Do not include `BEGIN_DRAFT_CHANGELOG` or any draft block. The recovered release
notes are authoritative; the harness's job here is to land the PR, not to
author. Stop after the last Audit bullet.

Release fact sheet:
{{ release_fact_sheet }}
