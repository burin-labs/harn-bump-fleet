A published release that was never folded is now a decided repair rather than
something a person has to notice. The decision names which half is outstanding,
the changelog section or the development version, and refuses to dispatch when
a fold pull request is already open. The chain's planning mode also now refuses
with a typed receipt naming the effect it declined, instead of raising an
exception an operator has to read as a stack trace.
