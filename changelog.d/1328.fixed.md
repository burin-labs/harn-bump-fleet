Wait out an exhausted GitHub API budget in the consumer rehearsal when the reset falls inside the job's own timeout, and when it does not, fail naming the reset time and the budget left instead of only the refusal; report what each run spent.
