The daily branch audit now fails only when it could not measure a repository or could not write an issue. Finding branches without a pull request is the audit's product, not a failure, so a fully measured sweep succeeds and still reports the count. A gating caller that wants any finding to stop it can pass `--fail-on-findings`.
