Standing projection-convergence pull requests now carry an age, and a proposal
that sits past a three-day budget turns the scheduled run red. Auto-merge
refusals and a sweep that cannot list open proposals fail the same way, so
those outcomes cannot hide in an artifact while the workflow stays green.
