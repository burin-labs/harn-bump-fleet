`release_harn` certification waits and `watch_harn_release` observation now call `std/lifecycle/progress`, so each long stage prints a start line with its ceiling, a ~60s heartbeat with observable state, and exactly one terminal line.
