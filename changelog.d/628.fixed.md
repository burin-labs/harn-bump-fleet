Compute the exact-pin certification digest with the `sha256` builtin instead of an undeclared `harness.crypto` capability, which panicked the live candidate certification lane and blocked the v0.10.53 signed tag. The digest now sits behind `exact_pin_binary_digest`, so the non-mock path is exercised by tests rather than first executing during a release.
