Release-readiness review loops now declare a provider fallback chain and keep equivalent-model failover when the primary route is unavailable, instead of pinning a single OpenRouter cell with an empty chain.
