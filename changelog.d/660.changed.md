Rewrite blocked bot PR heads through GitHub's own signing instead of a local key. `sign_bot_prs.harn` now publishes the prepared worktree with `createCommitOnBranch`, leased on the head it planned against, so a workflow holding no signing key can unblock its own required-signatures PR. Requires `harn-github-connector` v0.8.2, where the rewrite stops passing through the base commit and closing the pull request it is rewriting.
