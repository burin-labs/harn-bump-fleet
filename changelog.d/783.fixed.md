Hosted release publication proof now receives the GitHub App inputs required
to refresh its short-lived token. A typed workflow regression checks every
token-refreshing step, so a later call site cannot silently omit either secret.
