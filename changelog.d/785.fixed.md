Hosted fleet convergence now arms Git's GitHub CLI credential helper from the
existing ephemeral release-bot token before materializing repositories. Private
fleet members clone without embedding credentials in URLs, arguments, or logs.
