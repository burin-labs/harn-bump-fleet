Wait for GitHub's pull request record to catch up to a rewrite before reading it. Publishing writes the branch and the commit separately, and the record catches up to each in turn, so a single read lands in the gap and the commits query fails outright. Reporting that as a failed rewrite sent the caller back to redo work GitHub had already accepted, where the retry would then refuse on its own lease.
