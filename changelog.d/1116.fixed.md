The lease reaper now clears a dead round's `harn-update-preflight/<chain id>` ref as well as its `harn-update-chain/<chain id>` ref. Both name the same chain and are owned by the same runs, but the reaper matched only the chain prefix, so preflight residue from a round that died mid-flight accumulated with nothing in the fleet able to clear it and the reaper reported the remote as holding no lease at all.
