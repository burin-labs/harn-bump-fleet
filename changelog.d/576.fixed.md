Release candidate certification now stops sibling lanes immediately after a terminal pre-tag failure,
cancels already-dispatched hosted runs, and records typed failure and cancellation receipts without
duplicating full evidence into the release transcript.
