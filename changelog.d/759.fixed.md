The confined Harn release launcher now grants only its dedicated sibling
release-workspace root, so live releases can materialize isolated worktrees
without disabling the sandbox or authorizing unrelated project worktrees.
Git-worktree dogfood tests also declare their shared I/O resource group, so
bounded-parallel suites do not turn host contention into timeout verdicts.
