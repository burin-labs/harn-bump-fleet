Runtime bumps for `notion-sdk-harn` now regenerate the SDK and its provenance
receipt as one refresh operation, then validate both the receipt and recorded
consumer smoke path before publishing a signed bump commit.
