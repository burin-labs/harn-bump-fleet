Consumer-owned bump contracts now stamp their SHA-256 identity into generated
adapters and release receipts. Live releases verify those adapters before any
publication work, so a manifest-only change fails before a release begins and
the deterministic convergence path can propose the exact paired repair.
