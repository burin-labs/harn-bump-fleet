Ref-plumbing pushes — archiving an abandoned release attempt, pointing a
certification branch at a published candidate — no longer run the checkout's
pre-push hook. Those pushes carry no local work, so the hook had no subject and
contributed only the current branch's tracking state, failing the very command
the release harness prints as the remedy for a wedged version.
