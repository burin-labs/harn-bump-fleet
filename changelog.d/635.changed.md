Fleet package CI now pins the shared workflow revision that carries the
release-integrity invariant, so "every `vX.Y.Z` tag has a published release
behind it" is a checked property in every package repository rather than a
convention. The state it prevents is a signed tag with nothing behind it, which
cannot be repaired in place: the version is burned.
