The bump-fleet audit summary now runs its validate->repair retry on harn's canonical `workflow_run_repair` primitive (inline executor) instead of a hand-rolled two-attempt loop, and the release audit/summary/repair agent loops resolve their shared provider/model/timeout/idle-timeout infra through one `release_agent_infra_defaults` helper in `lib/llm_defaults` rather than duplicating the resolution inline. Both are pure DRY cleanups: the resolved `agent_loop` options and the summary retry/fail-open behavior are byte-identical to before.
