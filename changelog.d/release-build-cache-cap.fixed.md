`release_harn` now prunes only its managed Cargo `build` directory when it grows past a bounded cap before prepare, preserving compiled dependency cache while preventing multi-run release targets from ballooning into local disk-pressure and APFS slowdown. Its base-branch preflight can also fast-forward a clean branch checked out in another worktree, while still refusing dirty or diverged owners.
