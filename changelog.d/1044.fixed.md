Judge a repository's bump adapter by its declared workflow ownership at dispatch, the same way the fleet audit does, so a repository-owned bump workflow that keeps its own schedule and job is no longer refused as a stale adapter. The unproven repositories and their reason now appear in the `Require exact update proof` failure line.
