- `admit_green.harn` admits one or more green pull requests at an exact head, dry-run by default. It carries the guards fleet control had been keeping in a scratchpad shell script: latest-per-name rollup reduction so a respawned check run stops reading as a permanent failure, an exact-name required-check lookup so a check that never reported cannot read as passed, a refusal to treat an empty rollup as a clean pull request, an exact 40-character head match, and a REST merge scoped to the one `gh` refusal it repairs.
