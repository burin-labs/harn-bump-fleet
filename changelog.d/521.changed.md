Release preparation now materializes one immutable candidate before
certification, then overlaps its hosted platform/local audit proof with the
Linux binary-size gate. Signed tag creation remains blocked until every lane
and the residual generated-content audit are green.
