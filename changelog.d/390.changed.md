Cut changelog fragment parsing, ordering, normalization, assembly, section
lookup, and SemVer validation over to typed Harn stdlib contracts. The local
module now retains only release-specific promotion, drift reconciliation,
archival, and gating policy.
