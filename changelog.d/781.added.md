Hosted fleet convergence can now run a bounded Harn agent repair loop for
failed downstream bump pull requests, validate each repair with the
manifest-owned command, and publish it under an exact branch-head lease.
