- Refuse a fresh or superseded Harn release candidate when crates.io already
  owns that version, recover the packaged source from `.cargo_vcs_info.json`,
  and never treat deleting a GitHub tag as authority to re-cut.
