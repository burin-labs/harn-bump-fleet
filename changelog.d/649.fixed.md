A hosted certification run that ends cancelled, stale, or startup-failed is
re-acquired instead of failing the release. Those conclusions are not the lane
judging the candidate, so treating one as a verdict spent a full release on a
queue eviction. A run that fails or times out still stands: that is the lane
reporting on the code.
