Fixed the daily scheduled update round dying during target resolution: the resolve step now carries the job's read-only token, so `gh` can ask the GitHub API for the latest Harn release when no explicit version input is present.
