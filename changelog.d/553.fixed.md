- Hardened release-note recovery so candidate resets tolerate an empty `changelog.d`, immutable checkpoints remain untouched, production uses the tested deterministic fold seam, and operator receipts derive from the same typed note policy.
