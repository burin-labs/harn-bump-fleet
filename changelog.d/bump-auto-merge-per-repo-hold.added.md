A repository can now withhold auto-merge arming on its runtime-bump pull request with `bump_enable_auto_merge = false`. Arming remains the fleet default; the override is for a repository where merging the bump decides something a person has reserved, so the pull request opens and runs its checks but waits to be armed. The live policy audit reads the same resolution, so an adapter that silently returns to arming is reported as drift.
