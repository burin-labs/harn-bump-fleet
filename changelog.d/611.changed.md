- Generate every Harn runtime-bump adapter from typed `fleet.toml` ownership and declared refresh and validation inputs. Package CI and bump adapters now share one exact workflow projection boundary.
