Hosted fleet propagation now records a repository it withheld because a prerequisite update had not landed yet. Previously the durable continuation receipt kept only repositories with an accepted workflow run, so a correctly deferred dependent vanished from the handoff; the next round read it as a missing bump pull request, refused it as not code-repairable, and stopped before proof. The deferral is persisted with the exact prerequisite it awaits, convergence keeps the dependent waiting instead of failing it, and a later round dispatches it once the prerequisite pin satisfies the target.
