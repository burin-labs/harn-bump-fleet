The pin-only package CI projection now moves the CI status action to the same
revision as the package workflow. The two are co-versioned by policy, so
rewriting one and not the other left every superset repository's workflow split
across two revisions — which surfaced only as a red policy check in all of them
at once, and never as anything local.
