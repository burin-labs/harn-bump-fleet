An audit for an older Harn release now preserves an open pull request that
advances the consumer beyond its current release. It still closes duplicate or
regressive bump pull requests and refuses unknown pins.
