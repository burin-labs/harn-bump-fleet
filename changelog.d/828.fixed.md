Fleet convergence now treats a consumer on a newer Harn release as converged,
closes redundant bump pull requests instead of dispatching a downgrade, and
reports an open pull request's own pin separately from its default branch.
