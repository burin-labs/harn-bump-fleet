The fleet dispatches projection convergence when a canonical source file moves, instead of leaving consumers stale until the next daily cron, and the consumer drift remedy now names the repositories to converge.
