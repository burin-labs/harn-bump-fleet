The hosted Harn bump fleet now recovers exact signed target branches that were
misclassified as no-ops and gives breaking downstream migrations a second
bounded, CI-fed value-model repair cycle before final convergence proof.
