Fixed `bump_fleet.harn` failing every repository with `TypeError: parameter
'name' expected string, got dict`. The capability migration threaded the clock
into `end_timing` but not `start_timing`, so no fleet bump could dispatch. The
timing helpers now declare `name: string` and `attrs: dict`, which makes `harn
check` reject the mis-ordered call statically.
