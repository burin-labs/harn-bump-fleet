Cut release and fleet GitHub automation directly over typed connector
contracts, with exact workflow identities, canonical absence/error evidence,
and expected-head publication leases. Removed CLI fallback, raw GraphQL,
JSON/stdout parsing, and run-list correlation from production paths.
