The exact-candidate-tree contract now scopes its changelog check to the fragments the candidate's own commit introduced. A fragment that lands on the default branch while a candidate is in flight appears in the merge queue's synthetic tree, but no longer rejects the candidate; it is deferred to the next release and named in the decision's reason. A fragment the candidate itself failed to fold still rejects.
