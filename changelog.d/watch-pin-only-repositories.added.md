Watch Harn pins in repositories the fleet cannot bump. A new `harn_pin_path`
manifest field opts a repository into the pin-staleness alarm and names where
its pin lives, so the Homebrew tap's formula version is now watched alongside
every `.harn-version` consumer.
