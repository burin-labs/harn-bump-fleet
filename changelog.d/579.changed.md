Fleet package policy now requires one co-versioned, typed CI status contract.
It rejects roll-ups that skip failed jobs, inspect only some dependencies, or
pin the status action independently from the canonical package workflow.
