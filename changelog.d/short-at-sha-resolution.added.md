Accept an abbreviated `at_sha` and widen it to the exact commit before anything downstream sees it. Seven to forty hex digits now name a commit, the preflight resolves it against the release repository, and a prefix that names no commit or does not unambiguously extend into one is refused with the reason. Everything after the resolution still receives the full forty characters, so the dispatch receipt, the certification branch, and the tag all name one commit.
