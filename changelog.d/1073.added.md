The release preflight now runs package test discovery for every managed package
under the candidate runtime. A package whose test files discover no tests is a
named compatibility failure with the migration named, rather than a green count
over a check that ran nothing. The fleet's pinned runtime moves to the release
that can perform that inventory.
