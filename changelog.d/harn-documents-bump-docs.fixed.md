The Harn documents package now regenerates its API documentation inside the runtime bump. Its manifest entry declared no refresh command, so a bump moved the runtime without re-rendering `docs/api.md`, and the package verification that follows refused the tree the bump had itself invalidated: pull request #31 sat unmerged from 09:58 on 2026-09-07 with `docs/api.md is stale; run 'harn package docs'`, leaving that repository the fleet's only stale pin.
