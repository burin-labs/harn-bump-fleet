Remote fleet convergence now repairs pin-only package CI while preserving each
repository's own jobs. Every Harn package must also declare whether the fleet
owns its whole CI workflow or only the shared workflow pins.
