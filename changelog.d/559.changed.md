Replace workflow substring probes with one typed, fail-closed GitHub Actions projection shared by fleet discovery and policy tests.
Bump discovery and fleet policy now reject adapters whose dispatched `version` input is missing or not a string.
