`release_harn` now names a shallow checkout when the release-body gate
cannot derive the pin-time notes. `git merge-base` against a shallow boundary
commit exits non-zero with an empty stderr, so a blocking gate reported only
"could not be derived" and neither the cause nor the remedy.
