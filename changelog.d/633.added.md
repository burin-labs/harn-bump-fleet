Added `.github/workflows/hosted-release.yml`, a manually dispatched workflow that runs `release_harn.harn` on a GitHub-hosted runner with a `bump` and `mode` selector. It moves the Rust compilation that dominates a release off the operator machine at no runner cost, since both repositories are public. Secrets live in a reviewer-protected `release` environment, and tag signing stays local.
