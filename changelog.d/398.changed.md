Delete the repo-local token scanner for unbound symbols and rely on Harn's
native `HARN-NAM-001` name-resolution diagnostic during the existing strict
whole-repository check.

Run the independent Harn test suite through the bounded parallel scheduler;
the complete suite is parallel-safe and no longer pays sequential process
startup costs in CI.
