`abandon_release_attempts.harn` now holds the `release-owner` host lease for its whole run, in both dry-run and apply, and refuses while a live release owns it. Every claim it checks is terminal, so a candidate still working toward its tag classified as an unclaimed orphan and could be archived out from under the release that was cutting it.
