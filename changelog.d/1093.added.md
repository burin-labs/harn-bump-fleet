The fleet now audits every declared repository daily for remote branches that have no open pull request. It publishes non-vacuous branch, pull-request, finding, and failure counts, keeps one durable issue in each affected repository, and never deletes branches.
