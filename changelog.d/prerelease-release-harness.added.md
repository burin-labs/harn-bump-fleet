Expose premajor, preminor, prepatch, and prerelease cuts through the local and
hosted release harnesses. Prerelease identifiers now flow through changelog,
immutable publication, certification, watch, and cleanup contracts, while fleet
bump automation remains stable-release-only.
