Select the sole immutable release attempt whose recovered source pin descends
from every other candidate pin. The harness prints the selected attempt and
pin; genuinely ambiguous histories now list every candidate and exact
`--at-sha` recovery command.
