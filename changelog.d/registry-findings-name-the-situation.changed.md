The check that keeps the hosted release failure registry in step with its
workflow now says what happened rather than only which step is involved. A step
with no row says a step was added or renamed and where the rows live; a row
with no step says a step was removed or renamed and offers to point the row at
its replacement. A rename produces both halves, which is the case that refused
the default branch and read as two unrelated riddles.
