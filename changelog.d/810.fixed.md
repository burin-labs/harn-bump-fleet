Declare the release harness's narrow GitHub API egress policy after loading local environment files, so durable post-publication watches cannot be blocked or widened by missing or ambient `HARN_EGRESS_*` configuration.
