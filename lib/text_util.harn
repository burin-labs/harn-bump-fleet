/** Convert escaped newlines in rendered markdown blocks back into real newlines. */
pub fn markdown_multiline(value: string?) -> string {
  return trim(regex_replace_all("\\\\n", "\n", value ?? ""))
}
